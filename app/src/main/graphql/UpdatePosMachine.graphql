mutation UpdatePosMachine($serialNumber: String, $plateNumber: String) {
    updatePosMachine(serialNumber: $serialNumber, plateNumber: $plateNumber) {
        message
        success
        data {
            linkedCar {
                id
            }
            status
        }
    }
}